name: flutter_base
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  # flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter
    #各种加载框
  flutter_spinkit: ^5.0.0

  shimmer: ^2.0.0-nullsafety.0 # 谁用谁闪亮

  event_bus: ^2.0.0
  provider: ^5.0.0 #State

  # dio: 3.0.9 #网络请求
  dio: ^4.0.0

  back_button_interceptor: ^5.0.0 #返回键拦截器
  # pull_to_refresh: ^1.6.4  #下拉刷新、上拉加载更多
  pull_to_refresh:
    path: ../flutter_pulltorefresh

  qr_flutter: ^4.0.0

  crypto: ^3.0.1 #hash
  convert: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
