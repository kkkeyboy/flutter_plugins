name: flutter_base
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter
    #各种加载框
  flutter_spinkit: ^4.1.2+1

  shimmer: ^1.1.1 # 谁用谁闪亮

  event_bus: ^1.1.1
  provider: ^4.1.3 #State

  # dio: 3.0.9 #网络请求
  dio: ^3.0.10

  back_button_interceptor: ^4.4.0 #返回键拦截器
  pull_to_refresh: 1.6.3 #下拉刷新、上拉加载更多
  qr_flutter: ^3.2.0

  crypto: 2.1.5 #hash

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
