name: json_model
description: Generate model class from Json file. partly inspired by json_model.
version: 1.4.1
homepage: https://github.com/fadhilx/json_model

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  path: ^1.8.0

  args: ^2.0.0
  build_runner_core: ^6.1.12

  io: ^1.0.0
  logging: ^1.0.1

  expressions: ^0.2.1
  build_runner: ^1.12.2
  json_serializable: ^4.0.1
  json_annotation: ^4.0.1

dev_dependencies:
  pedantic: ^1.11.0
  test: ^1.16.8
